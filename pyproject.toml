[project]
name = "mcpproject"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "langchain-groq>=1.1.3",
    "mcp-use>=1.7.0",
    "mcp[cli]<2",
]
